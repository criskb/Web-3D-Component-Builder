# 3D Component Builder

## Local development

Install dependencies:

```bash
npm install
```

Start the client (Vite):

```bash
npm run dev
```

Start the server (optional):

```bash
node src/server/index.js
```

> Note: `src/main.js` is a browser entry point and should not be executed directly with Node.js.
